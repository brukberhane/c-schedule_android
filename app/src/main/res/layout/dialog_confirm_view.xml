<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/bg_bottom_sheet"
    android:orientation="vertical"
    android:padding="15dp">

    <TextView
        android:id="@+id/dialog_view_query_text_view"
        android:layout_width="wrap_content"
        android:textSize="25sp"
        android:textStyle="bold"
        android:background="@null"
        android:layout_centerHorizontal="true"
        android:layout_alignParentTop="true"
        android:layout_height="wrap_content"
        tools:text="Are you sure about this id?"/>

    <Button
        android:id="@+id/dialog_view_no_button"
        android:layout_below="@id/dialog_view_query_text_view"
        android:layout_alignParentStart="true"
        android:layout_marginTop="50dp"
        android:layout_width="wrap_content"
        android:background="@null"
        android:layout_height="wrap_content"
        android:text="No"
        tools:text="No"/>

    <Button
        android:id="@+id/dialog_view_yes_button"
        android:layout_marginTop="50dp"
        android:layout_below="@id/dialog_view_query_text_view"
        android:layout_alignParentEnd="true"
        android:background="@null"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="YES"
        tools:text="Yes"/>

</RelativeLayout>